import 'package:flutter/material.dart';

class SetFeesScreen extends StatelessWidget {
  const SetFeesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Set Fees")),
      body: const Center(child: Text("Set Fees Screen")),
    );
  }
}
